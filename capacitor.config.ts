import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.nettruyen',
  appName: 'nettruyen',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
